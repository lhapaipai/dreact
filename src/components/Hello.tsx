import DReact from "dreact";

const world = "world";
export const Hello = (
  <div>
    <p className="paragraph container" style={{ width: 200, height: 300 }}>
      Hello <span>{world}</span> !
    </p>
  </div>
);
